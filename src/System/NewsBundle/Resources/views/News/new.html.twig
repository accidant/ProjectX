<div id="page-heading">
	<h1>Titel von Formulartest</h1>
</div>

<div class="content-box-outer">
	<div class="content-box">
		<div class="content-box-content">
			<form action="{{ path('news_create') }}" method="post" {{ form_enctype(main_content['form']) }}>
                        {{ form_widget(main_content['form']) }}
                        <p>
                            <button type="submit">Create</button>
                        </p>
                    </form>

                    <ul class="record_actions">
                        <li>
                            <a href="{{ path('news') }}">
                                Back to the list
                            </a>
                        </li>
                    </ul>
		</div>
	</div>
</div>